import { m } from "framer-motion";
import styled from "styled-components";

export const Seperator = styled(m.div)`
  background-color: var(--border-color);
  width: 100%;
  height: 1px;
  margin-top: 6rem;
  margin-bottom: 6rem;
`;
